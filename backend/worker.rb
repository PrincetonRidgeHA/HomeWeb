#!/usr/bin/env ruby
require 'rubygems'
require 'rest-client'
require 'json'

while(true)
    # Check for items in Redis
    while(true)
        # Run all tasks in MQ
    end
    # Wait sixty seconds before checking again
    sleep(60);
end